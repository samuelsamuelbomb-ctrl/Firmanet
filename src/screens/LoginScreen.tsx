/**
 * LoginScreen — Sign in / Sign up with email or Google.
 *
 * Ported from /routes/auth.tsx → renamed for navigation.
 * All business logic preserved, only route name changed.
 */

import AuthScreen from "./AuthScreen";
export default AuthScreen;